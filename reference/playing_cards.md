# Basic names of entities in a card deck.

Basic names of entities in a card deck.

## Usage

``` r
data("playing_cards", package = "MultiFactor")
```

## Format

`playing_cards`: A list with two character vectors; contains the four
`suits` and thirteen `ranks` of a standard poker deck. Used in the
vignette.

## Source

`playing_cards`: Manual input.
